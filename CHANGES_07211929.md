# Changes — 2026-07-21 (session 07211929)

This document has two parts:

1. **실행 가이드** — how to actually turn on and use the hf-local (in-process
   HuggingFace) backend for delegation sub-agents and auxiliary tasks, given
   everything fixed in the two commits below.
2. **최신 2개 커밋 변경 요약** — a combined summary of `ff46e13` and
   `5fb9ff9`, the two commits that got the hf-local backend from "computes
   but nothing reads it" / "tools silently dropped" to actually working
   end-to-end.

---

## Part 1 — Delegation Local Model 실행 가이드

### 1.1 무엇이 되는가

메인 에이전트는 그대로 openai `gpt-5.5` 등 원격 provider를 쓰면서, 아래 세
경로만 로컬 Qwen(`agent/hf_local/`, `api_mode="hf_local"`)으로 라우팅할 수
있다:

| 경로 | 설정 위치 | 슬롯 |
|---|---|---|
| Delegation sub-agent (`delegate_task`) | `delegation.provider: hf-local` | LLM (`hf_local.llm.model`) |
| Auxiliary 텍스트 태스크 (compression, mcp, title_generation 등) | `auxiliary.<task>.provider: hf-local` | LLM |
| Auxiliary vision (`tools/vision_tools.py`) | `auxiliary.vision.provider: hf-local` | VLM (`hf_local.vlm.model`, lazy-load) |

세 경로 모두 **같은 `HFWorkerPool`**(GPU당 워커 프로세스 1개, `CUDA_VISIBLE_DEVICES`
기준)을 공유한다. `hf_local.enabled: false`(기본값)면 이 경로들은 전혀
관여하지 않고 기존 동작이 그대로 유지된다 — 부모 프로세스에 torch/transformers가
import되는 일도 없다.

### 1.2 최소 설정 (config.yaml)

```yaml
hf_local:
  enabled: true
  dtype: bfloat16
  attn_implementation: eager   # 필수 — per-head attention capture 전제. 바꾸지 말 것.
  llm:
    model: Qwen/Qwen3.5-9B
    max_new_tokens: 2048
  vlm:
    model: Qwen/Qwen3-VL-8B-Instruct
    lazy: true                 # 첫 vision 요청 때만 로드

delegation:
  provider: hf-local           # sub-agent를 로컬 LLM으로

auxiliary:
  vision:
    provider: hf-local         # VLM 슬롯
  compression:
    provider: hf-local         # 필요한 태스크만 개별 지정 가능 (기본은 auto)
```

`profiling.enabled: true`를 추가하면 모든 HF-local 추론이 `<repo_root>/logs/`,
`<repo_root>/profiling/`에 turn 단위 json/txt/csv로 기록된다 (Part 2, `ff46e13`
참고). 프로파일링이 필요 없으면 꺼둬도 무방 — hf-local 라우팅 자체와는
독립적이다.

### 1.3 지금부터 실제로 동작하는 것 (이번 두 커밋으로 고쳐진 부분)

- **Vision → VLM 슬롯**: `auxiliary.vision.provider: hf-local`로 설정하면
  `tools/vision_tools.py`의 async 호출이 실제로 `Qwen3-VL-8B-Instruct`에
  도달한다. 이전에는 `async_call_llm`에 hf-local 분기가 없어 vision
  provider 해석에 실패하고 조용히 main provider로 폴백했다 (`5fb9ff9`).
- **Delegation sub-agent tool-calling**: `delegation.provider: hf-local`인
  child agent가 `delegate_task`로 받은 tools(파일 읽기/쓰기, terminal 등)를
  실제로 프롬프트에서 보고 `<tool_call>{...}</tool_call>`을 방출할 수 있다.
  이전에는 `tools`가 `_HFLocalClientShim.create()`에서 pool/worker/모델까지
  전달되지 않아 Qwen이 tool 존재 자체를 몰랐다 (`5fb9ff9`).
- **KV-cache 프로파일링이 실제로 쌓인다**: `profiling.enabled: true`일 때
  각 HF 추론이 `InferenceRecord`로 기록되고, turn이 끝나면(모든 sub-agent/aux
  호출 포함) json/txt/csv로 flush된다. decode phase probe row도 이제 실제로
  계산된다 — 이전에는 prefill만 계산되고 decode는 스킵됐다 (`ff46e13`).

### 1.4 알려진 제약 / 아직 안 고친 것

- **Sub-agent 실패 시 유료 API로 조용히 에스컬레이션**: child agent는 부모의
  `_fallback_chain`을 그대로 상속한다. `delegation.provider: hf-local`로
  설정해도, 로컬 추론이 실패하면(GPU OOM, 워커 크래시 등) fallback 체인을
  타고 원격 provider로 넘어갈 수 있다 — 비용 통제가 필요하면 지금은
  fallback 체인을 아예 비워서 delegation을 쓰거나(`delegation.model`/
  `delegation.provider`만 설정하고 메인 agent의 fallback을 최소화), 로그로
  모니터링해야 한다. `delegation.inherit_fallback: false` 같은 opt-out
  설정은 아직 없다 (제안만 된 상태, 미구현 — Part 2 하단 참고).
- **Child agent 생성 시 불필요한 컨텍스트 프로브**: `hf://local`이
  `is_local_endpoint()`에서 로컬로 판정돼 매 child 생성마다 로컬 서버
  컨텍스트 프로브가 시도된다. `hf://` 스킴이라 httpx가 즉시 실패하고
  fuzzy default(128K)로 넘어가므로 기능상 문제는 없지만, child 스폰마다
  약간의 지연이 낀다. 아직 가드를 추가하지 않았다.
- **`.txt` 프로파일링 리포트의 Layer Inventory 섹션은 비어 있다**: worker→
  parent IPC로 레이어 인벤토리를 넘기는 부분은 (의도적으로) 구현하지 않았다
  — writer는 빈 리스트를 정상 처리하므로 크래시는 없다.
- **VLM 요청에 tools를 주면 무시된다**: `LocalModel.generate()`가 VLM
  경로에서는 tools를 버리고 경고 로그만 남긴다 — Qwen-VL 프로세서 chat
  template이 tools kwarg를 지원하지 않기 때문. tool-calling이 필요한 요청은
  LLM 슬롯(`delegation.provider`/텍스트 aux task)으로만 라우팅해야 한다.

### 1.5 트러블슈팅 체크리스트

1. `hf_local.enabled: true`인데 여전히 원격 provider가 호출된다 →
   `delegation.provider`/`auxiliary.<task>.provider`가 정확히 `hf-local`
   (하이픈, `hf_local` 아님)인지 확인. provider 문자열은 `hf-local`,
   `api_mode`/config 섹션 이름은 `hf_local`로 서로 다르다.
2. Vision이 여전히 main provider로 가는 것 같다 → `auxiliary.vision.provider`
   확인. `5fb9ff9` 이전 버전이면 애초에 안 됐다.
3. Sub-agent가 tool을 안 쓴다 → `attn_implementation: eager`인지, 모델이
   Qwen3/3.5 계열(`<tool_call>` 포맷 지원)인지 확인. `5fb9ff9` 이전이면
   tools가 애초에 안 넘어갔다.
4. 프로파일링 CSV에 `phase=decode` 행이 없다 → `max_new_tokens`가 1 이상인
   요청이 실제로 있었는지 확인(prompt만 있고 생성 토큰이 0개면 decode 행이
   없는 게 정상). `ff46e13` 이전 버전이면 애초에 decode probe가 안 돌았다.
5. `<repo_root>/logs/`, `<repo_root>/profiling/`가 안 생긴다 →
   `profiling.enabled: true`인지 확인 — hf-local 라우팅과는 별개 플래그다.

---

## Part 2 — 최신 2개 커밋 변경 요약

### `ff46e13` — fixed tool call -> hf model inference V cache profiling logic

프로파일링 파이프라인의 두 단절 지점을 연결:

**recorder가 실행 경로에 연결됨** — `SessionRecorder`(`agent/interp/session_recorder.py`)
는 완성돼 있었지만 아무 production 코드도 호출하지 않아 `probe_rows`가
응답 객체에만 실렸다가 버려지고 있었다.
- `session_recorder.py`: `contextvars.ContextVar` 기반 "현재 turn" 컨텍스트
  (`set_current_turn_id`/`get_current_turn_id`/`clear_current_turn_id`) 및
  turn-scoped 스레드세이프 `next_call_order()` 추가.
- `run_agent.py`: `AIAgent.run_conversation`이 최상위 turn 진입점에서
  `start_turn`/`complete_turn`을 열고 닫음 (중첩 호출은 ambient turn을
  재사용, profiling.enabled=false면 완전 무비용).
- `agent_init.py` / `auxiliary_client.py`: HF 추론 1건마다 `InferenceRecord`
  생성 → `add_record`. `meta`(request_id/call_order/parallel_group/
  is_parallel)를 `pool.generate()`와 record 양쪽에 동일하게 채워 probe_rows와
  record가 같은 식별자를 공유하게 함.
- `tools/delegate_tool.py`: 부모 turn_id를 자식 스레드로 명시적 전파
  (contextvar는 스레드 자동 상속 안 됨), `register_pending`/`complete_pending`
  으로 완료 배리어 구성 — 부모 turn이 먼저 끝나도 모든 자식/aux record가
  flush될 때까지 기다림.

**decode probe가 실제로 계산됨** — `LocalModel.generate()`가 prefill만 probe
하고 decode는 "Simplified: skip decode probe" 주석과 함께 건너뛰고 있었다.
- `agent/hf_local/capture.py`: forward hook이 매 forward마다
  `storage[layer_idx]`를 덮어써서 생성이 끝나면 마지막 decode step 캡처만
  남던 문제를 수정 — `storage[layer_idx]`를 리스트로 바꿔 프리필+각
  decode step 캡처를 모두 누적. v_proj 훅과 attention 모듈 훅의 실행 순서
  문제(값이 이전 forward의 dict에 쓰이던 버그)도 `pending_v` 핸드오프로
  같이 수정.
- `agent/hf_local/runtime.py`: 누적된 캡처로부터 전체 시퀀스를 재조립
  (`_concat_v_states`/`_build_square_attn`/`_concat_block_outputs`)해
  `phase="decode"`로 `compute_probe_rows`를 추가 호출 — decode-window
  causal matmul 경로가 실제 데이터로 실행됨.

테스트: `tests/agent/hf_local/test_recorder_wiring.py`,
`tests/agent/hf_local/test_capture.py` 신규 추가. 회귀: hf_local 46종 +
delegate/turn_context 202종 + auxiliary_client 345종 + run_agent 전체
1919종 통과 (4건 실패는 `python-multipart`/`openai` SDK 버전 등 환경
문제로, stash 전후 동일하게 실패함을 확인해 이번 변경과 무관함을 검증).

### `5fb9ff9` — fixed some possible issues in delegation flow

async 경로 라우팅 누락과 tools 드롭, 두 가지 라우팅 버그를 수정:

**버그 #1 — async 경로에 hf-local 분기 누락**: `auxiliary_client.py`의
sync `call_llm`에는 hf-local 분기가 있었지만 `async_call_llm`(`vision_tools.py`
가 전부 이걸 씀)에는 없어서, vision을 hf-local로 설정해도
`resolve_vision_provider_client("hf-local")`가 실패 → auto/main으로 조용히
폴백해 VLM 슬롯에 절대 도달하지 못했다. `async_call_llm`에 `task=="vision"`
분기보다 앞서 `resolved_provider == "hf-local"` 분기를 추가하고, blocking
함수인 `_call_hf_local_aux`를 `asyncio.to_thread`로 감싸 이벤트 루프를
막지 않게 함.

**버그 #2 — tools가 chat template까지 전달 안 됨**: `_HFLocalClientShim.create()`
가 받은 `tools`를 5개 레이어(shim → `pool.py::HFWorkerPool.generate` →
`worker.py` → `runtime.py::LocalModel.generate` → `apply_chat_template`)
전부에서 드롭하고 있어 Qwen이 tool 존재를 프롬프트에서 못 보고 delegation
sub-agent의 tool-calling이 동작하지 않았다. 5개 레이어 전부에 `tools`를
관통시킴 (전부 keyword-only, 기본값 `None` — 하위호환 유지). VLM 경로는
tools를 무시하고 경고 로그만 남김(processor chat template 미지원).
일관성을 위해 `auxiliary_client.py`의 `_call_hf_local_aux`/`call_llm`/
`async_call_llm`도 `tools`를 받아 넘길 수 있게 통로만 열어둠.

**부수 문제 (설계 권고만, 미구현)**: (A) delegation.provider=hf-local일 때
부모 fallback 체인 상속을 끊는 옵션 (`delegation.inherit_fallback`),
(B) `hf://local` base_url이 `is_local_endpoint()`에서 True 판정돼 매 child
생성마다 불필요한 로컬 서버 컨텍스트 프로브가 도는 것을 막는 가드. 둘 다
기존 동작을 바꾸지 않기 위해 사용자 확인 후 진행하기로 하고 코드 변경은
하지 않음.

테스트: `tests/agent/hf_local/test_hf_local_routing.py` 신규 10건
(async 라우팅 3건, tools 관통 shim/pool/worker/runtime 레이어별 5건,
VLM 무시+경고 1건, sync/async tools 통과 1건). 회귀: hf_local 66종(기존
56 + 신규 10) + config/runtime_provider/provider_catalog/aux_config
327종 + auxiliary_client 345종 전부 통과.
